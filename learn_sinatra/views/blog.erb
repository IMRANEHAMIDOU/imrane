<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AphaSoft</title>
    <script src="/index.js"></script>
</head>

<body class="container bg-black text-white py-4 px-40">
    <h1 class="text-[50px]">Bienvenu sur mon blog</h1>
    <div class="w-full mt-6">
       
        <div class="flex justify-between items-center py-4">
            <a href="/" class="bg-orange-500 text-white px-4 py-2 rounded hover:bg-orange-600">
                Accueil
            </a>
            <h1 class="text-3xl font-bold text-center">Liste des Posts</h1>
            <div class="text-right">
                <a id="addPost" href="/blog/add" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">
                    Ajouter un Post
                </a>
            </div>
        </div>

        <!-- Liste des posts -->
        <div id="postsList" class="space-y-4">
            <% posts.each do |post| %>
                <div class="bg-white p-6 rounded-lg shadow-md">
                    <h2 class="text-xl font-semibold mb-2 text-black"><%= post["title"] %></h2>
                    <p class="text-gray-700 mb-4">
                        <%= post["content"][0..100] %>...
                    </p>
                    <div class="flex space-x-4">
                        <a href="/blog/post/<%= post['id'] %>/edit" class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600">
                            Modifier
                        </a>
                        <a href="/blog/post/<%= post['id'] %>/delet" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">
                            Supprimer
                        </a>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</body>

</html>
