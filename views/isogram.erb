<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AphaSoft</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>

<body class="bg-black text-white py-4 px-4 sm:px-10 md:px-20 lg:px-40">
    <div class="container mx-auto">
        <!-- Titre -->
        <h1 class="text-2xl sm:text-3xl md:text-4xl lg:text-[35px] text-center sm:text-left">
            Bienvenu sur mon programme permettant de déterminer si un mot ou une phrase est un isogram ...
        </h1>

        <!-- Formulaire -->
        <form action="" method="post"
            class="max-w-full mt-6 p-4 bg-white rounded-lg shadow-md flex flex-col sm:flex-row items-center gap-4">
            <input type="text" placeholder="Votre phrase ici ..." name="world" value="<%= world %>"
                class="w-full sm:flex-1 text-black p-3 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-400 transition">
            <button type="submit"
                class="w-full sm:w-auto px-6 py-3 bg-blue-500 text-white font-semibold rounded-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-400 transition">
                Déterminer
            </button>
        </form>

        <!-- Résultat -->
        <% if iso %>
            <p class="text-lg text-green-300 my-4 text-center sm:text-left">
                <%= world %> est un isogram
            </p>
        <% else %>
            <p class="text-lg text-red-300 my-4 text-center sm:text-left">
                <%= world %> n'est pas un isogram
            </p>
        <% end %>

        <!-- Bouton Accueil -->
        <div class="mt-10 text-center sm:text-left">
            <a href="/"
                class="inline-block px-6 py-3 bg-blue-500 text-white font-semibold rounded-md hover:bg-blue-600 transition">
                Accueil
            </a>
        </div>
    </div>
</body>

</html>